export function TextTitle1({ children }: { children: React.ReactNode }) {
  return (
    <h1 className="text-[26px] font-bold text-white tracking-tight leading-tight">
      {children}
    </h1>
  );
}
